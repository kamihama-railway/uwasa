# Uwasa 项目待办事项

## 已完成事项 (Done)
- [x] 项目初始化 (Go 1.25)
- [x] 定义 `Context` 接口及 `MapContext` 实现
- [x] 实现词法分析器 (Lexer)
- [x] 实现基于 Pratt 算法的语法分析器 (Parser)
- [x] 实现 AST 解释器 (Evaluator)
- [x] 支持 `&&` 运算符的短路逻辑
- [x] 实现 `if...is...else` 分支语法
- [x] 实现 `if...then` 前置条件语法
- [x] 实现变量赋值功能
- [x] 完善单元测试覆盖
- [x] 编写使用文档和设计文档
- [x] 增加性能测试 (Benchmark)
- [x] 配置 GitHub Actions CI 工作流 (自动化测试 & 性能追踪)

## 计划目标 (Future Goals)
- [ ] **性能优化**: 引入字节码虚拟机以提高执行效率。
- [ ] **内置函数**: 支持 `min`, `max`, `len` 等常用内置函数。
- [ ] **更多类型**: 增加对数组 (Array) 和对象 (Map) 字面量的支持。
- [ ] **更多运算符**: 增加乘法 `*`、除法 `/` 等算术运算符。
- [ ] **错误处理增强**: 提供更精确的错误位置（行号、列号）提示。
- [ ] **类型检查**: 引入可选的静态类型检查阶段。
- [ ] **循环支持**: 考虑是否需要引入简单的循环语法。
- [ ] **并发安全**: 确保 `Context` 在并发执行下的安全性。
